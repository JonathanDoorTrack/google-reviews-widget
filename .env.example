# Google Places API key (from Google Cloud Console > Credentials > API Key)
GOOGLE_PLACES_API_KEY=your-api-key

# Your Google Place ID (find at https://developers.google.com/maps/documentation/places/web-service/place-id)
GOOGLE_PLACE_ID=your-place-id

# Railway sets PORT automatically — only needed for local dev
PORT=3000
